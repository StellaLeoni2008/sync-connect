import {createFileRoute}from"@tanstack/react-router";import{useEffect,useState}from"react";import{AppShell}from"@/components/layout/app-shell";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Switch}from"@/components/ui/switch";import{supabase}from"@/integrations/supabase/client";
export const Route=createFileRoute("/_authenticated/you")({component:You});function You(){const{user}=Route.useRouteContext();const[prefs,setPrefs]=useState({strong_sync:true,mutual_sync:true,help_alert:true,resync:true,haptics:true});const[name,setName]=useState("");const[tags,setTags]=useState({hobbies:"",interests:"",can_help_with:""});const[saved,setSaved]=useState(false);useEffect(()=>{supabase.from("profiles").select("name,hobbies,interests,can_help_with").eq("id",user.id).single().then(({data})=>{setName(data?.name??"");setTags({hobbies:(data?.hobbies??[]).join(", "),interests:(data?.interests??[]).join(", "),can_help_with:(data?.can_help_with??[]).join(", ")})});supabase.from("notification_preferences").select("strong_sync,mutual_sync,help_alert,resync,haptics").eq("user_id",user.id).maybeSingle().then(({data})=>{if(data)setPrefs(data)})},[user.id]);async function change(k:keyof typeof prefs,v:boolean){const n={...prefs,[k]:v};setPrefs(n);await supabase.from("notification_preferences").upsert({user_id:user.id,...n})}async function saveTags(){const list=(value:string)=>value.split(",").map(item=>item.trim()).filter(Boolean);await supabase.from("profiles").update({hobbies:list(tags.hobbies),activities:list(tags.hobbies),interests:list(tags.interests),can_help_with:list(tags.can_help_with)}).eq("id",user.id);setSaved(true)}
return <AppShell light><div className="pt-12"><p className="text-xs uppercase text-muted-foreground">You</p><h1 className="mt-3 text-4xl font-medium">{name||"Your profile"}</h1><div className="mt-10 rounded-2xl border border-border bg-card p-5"><h2 className="font-medium">Notifications & haptics</h2>{Object.entries(prefs).map(([k,v])=><label key={k} className="flex items-center justify-between border-b border-border py-4 last:border-0"><span className="text-sm">{k.replaceAll("_"," ")}</span><Switch checked={v} onCheckedChange={x=>change(k as keyof typeof prefs,x)}/></label>)}</div><div className="mt-3 rounded-2xl border border-border bg-card p-5"><h2 className="font-medium">What SYNC can match you on</h2><p className="mt-2 text-sm text-muted-foreground">Separate with commas. Hobbies and interests count as much as skills.</p><Input className="mt-4 h-13 rounded-full px-5" value={tags.hobbies} onChange={e=>{setSaved(false);setTags({...tags,hobbies:e.target.value})}} placeholder="Hobbies & activities"/><Input className="mt-3 h-13 rounded-full px-5" value={tags.interests} onChange={e=>{setSaved(false);setTags({...tags,interests:e.target.value})}} placeholder="Interests"/><Input className="mt-3 h-13 rounded-full px-5" value={tags.can_help_with} onChange={e=>{setSaved(false);setTags({...tags,can_help_with:e.target.value})}} placeholder="You can help with"/><Button className="mt-4" variant="outline" onClick={saveTags}>{saved?"Saved":"Save"}</Button></div><div className="mt-3 rounded-2xl border border-border bg-card p-5"><h2 className="font-medium">SYNC Band</h2><p className="mt-2 text-sm text-muted-foreground">Your phone has everything you need. Band setup requires the native app.</p><a href="/band" className="mt-5 inline-block text-sm underline">Explore the Band</a></div><Button variant="outline" className="mt-8" onClick={async()=>{await supabase.auth.signOut();window.location.href="/"}}>Sign out</Button></div></AppShell>}