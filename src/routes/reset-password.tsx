import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { SyncLogo } from "@/components/brand/sync-logo";
import { supabase } from "@/integrations/supabase/client";
export const Route = createFileRoute("/reset-password")({ head: () => ({ meta: [{ title: "Reset password — SYNC" }, { name: "description", content: "Choose a new password for your SYNC account." }, { property: "og:title", content: "Reset password — SYNC" }, { property: "og:description", content: "Secure your SYNC account." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: ResetPassword });
function ResetPassword(){ const navigate=useNavigate(); const [password,setPassword]=useState(""); const [message,setMessage]=useState(""); async function update(){ if(!window.location.hash.includes("type=recovery")){setMessage("Open the recovery link from your email first.");return;} const {error}=await supabase.auth.updateUser({password}); if(error)setMessage(error.message); else navigate({to:"/sync"}); } return <main className="dark min-h-screen bg-background px-5 py-12 text-foreground"><div className="mx-auto max-w-sm"><SyncLogo className="text-2xl"/><h1 className="mt-32 text-4xl font-medium">Choose a new password.</h1><Input className="mt-8 h-13 rounded-full bg-card px-5" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><Button className="mt-3 w-full" size="lg" onClick={update}>Update password</Button>{message?<p className="mt-4 text-sm text-muted-foreground">{message}</p>:null}</div></main> }